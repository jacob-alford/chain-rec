/**
 * Re-exports chainRec from fp-ts > Identity
 *
 * @since 1.0.0
 */
import { ChainRec } from 'fp-ts/Identity'

/**
 * A re-export of fp-ts > Identity.chainRec
 *
 * @since 1.0.0
 * @category Model
 */
export const chainRec = ChainRec.chainRec
